<%= form_for(nasdaq_instrument) do |f| %>
  <% if nasdaq_instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nasdaq_instrument.errors.count, "error") %> prohibited this nasdaq_instrument from being saved:</h2>

      <ul>
      <% nasdaq_instrument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :symbol %>
    <%= f.text_field :symbol %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
